from django.contrib import admin

# Change the admin site header
admin.site.site_header = "Vista.One Admin Panel"

# Change the admin site title (appears on browser tab)
admin.site.site_title = "Vista.One"

# Change the index title (appears on the admin home page)
admin.site.index_title = "Welcome Vista.One Member"
